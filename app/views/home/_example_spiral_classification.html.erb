      <div class="container">
        <hr>
        <h3>Example - <small class="text-muted">Spiral dataset classification using python machine learning</small></h3>
        <br><h5>Train a neural network on three classes of data in a spiral pattern.</h5>
        <br><p>Three classes are generated forming a spiral pattern moving out from a centre point in 2-dimensional space. This example trains a neural network classifier on the three classes, and shows the predictions of the neural network on the whole space.</p>
<p>
<%= form_for :task, url: tasks_path do |f| %>
<%=   f.hidden_field(:input, value: File.read('examples/spiral-classification.json')) %>
<%=   f.submit 'Load', class: 'btn btn-primary btn-md' %>
<% end %>
</p>

      </div>
